"use client";
import { Canvas } from "@react-three/fiber";
import { MeshDistortMaterial, Sphere, Float } from "@react-three/drei";
import { motion } from "framer-motion";

const container = {
  hidden: { opacity: 0 },
  show: { opacity: 1, transition: { staggerChildren: 0.2 } },
};

const item = {
  hidden: { y: "100%", skewY: 10 },
  show: { y: 0, skewY: 0, transition: { duration: 0.8, ease: [0.76, 0, 0.24, 1] } },
};

export default function Hero() {
  return (
    <section className="relative h-screen w-full flex items-center justify-center overflow-hidden">
      {/* 3D 背景 */}
      <div className="absolute inset-0 z-0 opacity-50">
        <Canvas>
          <ambientLight intensity={0.5} />
          <Float speed={2} rotationIntensity={1} floatIntensity={2}>
            <Sphere args={[1, 64, 64]} scale={2.5}>
              <MeshDistortMaterial color="#ffffff" distort={0.5} speed={2} roughness={0.1} />
            </Sphere>
          </Float>
        </Canvas>
      </div>

      {/* 标题 */}
      <motion.div
        variants={container}
        initial="hidden"
        animate="show"
        className="relative z-10 text-center"
      >
        <div className="overflow-hidden">
          <motion.h1 variants={item} className="text-[15vw] font-bold tracking-tighter uppercase leading-[0.8]">
            Digital
          </motion.h1>
        </div>
        <div className="overflow-hidden">
          <motion.h1 variants={item} className="text-[15vw] font-bold tracking-tighter uppercase leading-[0.8]">
            Craft
          </motion.h1>
        </div>
      </motion.div>
    </section>
  );
}
